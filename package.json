{
  "name": "just-cancel-mcp-node",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Just Cancel — discover which subscriptions you should cancel to save money.",
  "keywords": [
    "subscription management",
    "cancel subscriptions",
    "money saving",
    "budget helper"
  ],
  "scripts": {
    "start": "tsx src/server.ts",
    "build": "cd web && pnpm run build && cd .. && node scripts/copy-pdf-worker.js && node scripts/inline-bundle-encoded.js",
    "build:web": "cd web && pnpm run build",
    "build:inline": "node scripts/inline-bundle-encoded.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "dotenv": "^17.2.3",
    "pdfjs-dist": "^5.4.530",
    "tsx": "^4.19.2",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "@types/node": "^24.10.1",
    "esbuild": "^0.27.0",
    "typescript": "^5.6.3"
  }
}